// Task
// Write a function that returns both the minimum and maximum number of the given list/array.

const minMax = (arr) => [Math.min(...arr), Math.max(...arr)];
// using spread syntax '...' because min and max only work for LISTS
